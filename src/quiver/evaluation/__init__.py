"""Evaluation harness: runs the real agents against golden cases and scores them.

This is the live, billed, on-demand quality layer — distinct from the offline
`tests/` suite, which verifies plumbing with stubbed agents.
"""
